# Godot 4 開発ルール

## 開発ルール
- 開発時はSPEC.mdの仕様に沿うこと
- 私から開発の指示があった場合、該当仕様についてSPEC.mdの更新を行うこと
- 一つの指示や開発の機能ごとにGithubにコミットをすること

## 一連のタスク完了後
- 指示された一連のタスクが全て終わった際に、以下を実施する
    - 私がすぐにテストプレイできるようにGodotのゲームのプレイ画面を起動すること
    - ビルドをGithub PagesにアップしてWebのURLを提示すること。その際、クリックしてそのままプレイできるようにその行はURL以外は書かないこと


## 定型コマンド
- テスト実行: `godot --headless -s res://addons/gut/gut_cmdln.gd`
- Webビルド: `godot --headless --export-release "Web" ./build/index.html`

## コーディング規約
- 言語: GDScript (Godot 4.x API)
- 機能を追加・変更した際は、必ず GUT 用のテスト (`test/` フォルダ内) を追加・更新すること。
- 修正完了時は、必ず上記のテストコマンドを実行してエラーが出ないことを確認すること。